algoritmo "contagem"
var
   contador: inteiro
inicio
   contador <- 10
   enquanto (contador >= 0) faca
      escrevaL (contador)
      contador <- contador - 1
   FimEnquanto
   escrevaL (" Acabou a contagem! ")
fimalgoritmo